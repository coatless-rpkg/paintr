## Resubmission

This is a resubmission. In response to the reviewer, the examples for the
internal `paint_format()` no longer use `paintr:::`. As an internal helper, its
illustrative examples were removed rather than exported.

## Test environments

* local macOS, R release
* GitHub Actions: ubuntu-latest, macOS-latest, windows-latest (R release and devel)
* win-builder (R devel and release)

## R CMD check results

0 errors | 0 warnings | 1 note

* This is a new submission.

* Depending on the platform, a second NOTE may report the installed package
  size. The vignettes render a number of full-resolution figures, so the
  installed size can approach or exceed 5 MB. The figures are integral to a
  package about visualising data structures.
