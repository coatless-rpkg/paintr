% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list.R
\name{paint_list}
\alias{paint_list}
\alias{gpaint_list}
\title{Visualize Data Inside of a List}
\usage{
paint_list(
  data,
  summarise = FALSE,
  show_indices = "none",
  highlight_area = NULL,
  highlight_color = "lemonchiffon",
  graph_title = paste0("Data Object: ", deparse(substitute(data))),
  graph_subtitle = NULL,
  sigfig = 3L,
  subtle_digits = c("insignificant", "rounded", "none"),
  max_chars = 12L,
  max_rows = 10L,
  max_cols = 8L,
  show_all = FALSE,
  fontsize = NULL,
  family = "mono",
  show_types = TRUE,
  show_names = TRUE,
  name_align = c("center", "left", "right"),
  type_align = c("center", "left", "right")
)

gpaint_list(
  data,
  summarise = FALSE,
  show_indices = "none",
  highlight_area = NULL,
  highlight_color = "lemonchiffon",
  graph_title = paste0("Data Object: ", deparse(substitute(data))),
  graph_subtitle = NULL,
  sigfig = 3L,
  subtle_digits = c("insignificant", "rounded", "none"),
  max_chars = 12L,
  max_rows = 10L,
  max_cols = 8L,
  show_all = FALSE,
  fontsize = NULL,
  family = "mono",
  show_types = TRUE,
  show_names = TRUE,
  name_align = c("center", "left", "right"),
  type_align = c("center", "left", "right")
)
}
\arguments{
\item{data}{A bare \code{list}. A list carrying a class --
\code{as.POSIXlt(Sys.time())}, an \code{lm}, a \code{t.test()} result --
is refused: \code{is.list()} is TRUE for all of them, but a
datetime drawn as eleven ragged columns of \code{sec}, \code{min},
\code{hour}, ... is a wrong picture, not a picture of a list.}

\item{summarise}{Draw every element as ONE cell saying what it is
(\verb{<int [3]>}, \verb{<chr [1]>}) instead of one cell per value.
Default: \code{FALSE}. It is the structure of the list at a
glance, and it is what a 40-element list wants.}

\item{show_indices}{Draw \verb{[[j]][i]} under each value (\code{"cell"} or \code{"all"}), or
nothing (\code{"none"}, the default).

\code{l[[2]][3]} is the expression students get wrong most often -- \code{l[2]} is a
list, \code{l[[2]]} is the vector, \code{l[[2]][3]} is the value -- and this lane prints
it under the number it returns. A summarised element gets \verb{[[j]]}, because that
is the accessor that returns the element itself.

There is deliberately \strong{no} \verb{[i]} gutter down the left. In a data frame,
reading across a row is the whole point: \code{df[[1]][2]} and \code{df[[2]][2]} are one
record. \strong{In a list that is false}, and a lane that teaches it would be a lane
that teaches a falsehood.}

\item{highlight_area}{Logical matrix marking the cells to fill: POSITIONS by
ELEMENTS, as deep as the deepest element. Build it with
\code{\link[=highlight_columns]{highlight_columns()}} (which selects elements, by name as
well as by number) and \code{\link[=highlight_rows]{highlight_rows()}} (which selects
positions within them). A length-one logical is recycled.
Default: \code{NULL}, which highlights nothing.}

\item{highlight_color}{Color to use to fill the background of a cell.}

\item{graph_title}{Title to appear in the upper left hand corner of the graph.}

\item{graph_subtitle}{Subtitle to appear immediately under the graph title.
\code{NULL} (the default) describes the data: how many elements,
the range of their lengths, and the class. \code{NA} or \code{""}
draws no subtitle.}

\item{sigfig}{Significant digits drawn in black. Digits past the
\code{sigfig}-th are drawn in grey; nothing is discarded.
Must be in \code{1:15}.}

\item{subtle_digits}{Which digits are drawn grey. \code{"insignificant"} (the
default) greys everything past the \code{sigfig}-th
significant digit; \code{"rounded"} greys only digits the
rendering actually lost; \code{"none"} draws everything black.}

\item{max_chars}{Strings longer than this are truncated with an ellipsis.}

\item{max_rows}{Elide the middle of an element longer than this. Default:
\code{10}. It is asked of each element SEPARATELY, so a length-2
element beside a length-40 one draws no \code{"..."} -- it is
hiding nothing.}

\item{max_cols}{Elide the middle of the list when it has more elements than
this. Default: \code{8}, because a list's columns are as wide as
a name.}

\item{show_all}{Draw every cell, however small the text becomes. Warns
when the text falls below the legibility floor.}

\item{fontsize}{Font size in points. \code{NULL} (the default) fits the text
to the device.}

\item{family}{Font family. \code{"mono"} by default.}

\item{show_types}{Draw the type-tag row (\verb{<dbl>}, \verb{<chr>}, ...) under the
element names. Default: \code{TRUE}.}

\item{show_names}{Draw the element-name row. Default: \code{TRUE}. A named
element is labelled \verb{$a}; an unnamed one is labelled
\verb{[[2]]}, exactly as \code{print()} does it. (This per-element
fallback is the one place in the package where a label lane
holds two kinds of thing at once -- a list is the only
structure whose parts are named one at a time.)}

\item{name_align}{How the element names sit over their column: \code{"center"}
(the default), \code{"left"} or \code{"right"}.}

\item{type_align}{How the type tags sit over their column. Independent of
\code{name_align}. Neither moves the values, which keep their
own alignment.}
}
\value{
\code{paint_list()} invisibly returns the resolved cell table. See \code{\link[=paint_matrix]{paint_matrix()}}
for its components.
\code{gpaint_list()} returns a \code{ggplot} object.
}
\description{
Generate a graph showing the contents of a list.
}
\details{
\strong{This picture does not draw NESTING.} A sublist is drawn as a single grey token
saying what it is -- \verb{<list [2]>} -- and its contents are not drawn at all.
Nesting is the hard part of lists (\code{l$a$b}, "why is my result a list of lists"),
and this painter refuses to teach it rather than teach it badly: a geometric
sub-grid inside a cell would not extend the layout engine, it would delete it
(every column has ONE width, indexed by integer column, and a fractional row is
silently truncated). The refusal is deliberate and it is stated here rather than
discovered in a picture. The same is true of any element that is not a plain
one-dimensional vector: a matrix element draws as \verb{<int [2 x 2]>}, a data frame
element as \verb{<df [5 x 3]>}.

\code{paint_list()} draws on the current base graphics device. \code{gpaint_list()} returns
a \code{ggplot} object.

\strong{A data frame IS a list whose elements happen to share a length.} That is what
this picture is for. Draw \code{paint_data_frame(data.frame(a = 1:3, b = 4:6))} and
\code{paint_list(list(a = 1:3, b = 4:6))} side by side and they are the same picture;
then draw \code{paint_list(list(a = 1:3, b = 4))} and watch the rectangle break. The
shared length is the only thing a data frame adds, and the ragged block is the
proof.

Each element is its own formatting unit, exactly as a data frame's column is: a
\code{1e15} in one element will not flip another into scientific notation.
}
\section{The ragged block has no outline}{

Every other painter draws a heavy border around the block of values. A list does
not, and the reason is that the block is not a rectangle. The heavy border would
be the BOUNDING BOX of the drawn cells, so on a 4/1/3 list it would run down to
the bottom of the deepest element and the length-1 element would sit at the top of
a tall, empty, heavily-boxed column -- a box drawn around cells that do not exist.
The cells keep their own borders, so the block still reads as a block; it just
reads as the ragged block it actually is.
}

\section{The ggplot object is a shell}{

\code{gpaint_list()} returns a real \code{ggplot} object -- \code{+ theme()}, \code{ggsave()},
\code{print()} and knitr chunks all work -- but its panel is drawn \emph{entirely} by a
custom grid grob, held in a single \code{annotation_custom()} over a meaningless
\verb{0..1} coordinate system. There is no \code{aes()}, no geom and no scale carrying any
meaning, so \code{ggplot_build()} sees an empty layer and \verb{+ scale_fill_*()} has no
effect on the drawing. Use \code{highlight_area} and \code{highlight_color} to fill cells.
}

\examples{
# Base graphics

# A list is a bag of vectors, and they need not be the same length.
paint_list(list(a = 1:4, b = "x", c = c(TRUE, FALSE, NA)))

# A data frame IS a list whose elements happen to share a length. These two
# pictures are the same picture.
paint_list(list(a = 1:3, b = 4:6))
paint_data_frame(data.frame(a = 1:3, b = 4:6))

# An unnamed element is labelled the way print() labels it.
paint_list(list(1:3, b = letters[1:2]))

# The accessor students get wrong, printed under the value it returns.
paint_list(list(a = 1:4, b = c(2.5, 3.5)), show_indices = "cell")

# A sublist is NOT drawn. It says what it is and stops there.
paint_list(list(a = 1:3, b = list(1, 2)))

# The structure of a long list, at a glance.
paint_list(list(a = 1:40, b = letters, c = matrix(1:4, 2)), summarise = TRUE)

# Highlight an element by name.
l <- list(a = 1:4, b = "x", c = c(TRUE, FALSE, NA))
paint_list(l, highlight_area = highlight_columns(l, "c"))
\dontshow{if (requireNamespace("ggplot2", quietly = TRUE)) withAutoprint(\{ # examplesIf}
# ggplot2 graphics ----

gpaint_list(list(a = 1:4, b = "x", c = c(TRUE, FALSE, NA)))

gpaint_list(list(a = 1:40, b = letters), summarise = TRUE)
\dontshow{\}) # examplesIf}
}
