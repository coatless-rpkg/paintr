% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format.R
\name{paint_format}
\alias{paint_format}
\alias{paint_format.double}
\alias{paint_format.integer}
\alias{paint_format.character}
\alias{paint_format.logical}
\alias{paint_format.factor}
\alias{paint_format.list}
\alias{paint_format.default}
\title{Format a vector for painting}
\usage{
paint_format(
  x,
  sigfig = 3L,
  max_chars = 12L,
  max_dec_width = 13L,
  subtle_digits = c("insignificant", "rounded", "none"),
  ellipsis = "...",
  ...
)

\method{paint_format}{double}(
  x,
  sigfig = 3L,
  max_chars = 12L,
  max_dec_width = 13L,
  subtle_digits = c("insignificant", "rounded", "none"),
  ellipsis = "...",
  ...
)

\method{paint_format}{integer}(
  x,
  sigfig = 3L,
  max_chars = 12L,
  max_dec_width = 13L,
  subtle_digits = c("insignificant", "rounded", "none"),
  ellipsis = "...",
  ...
)

\method{paint_format}{character}(
  x,
  sigfig = 3L,
  max_chars = 12L,
  max_dec_width = 13L,
  subtle_digits = c("insignificant", "rounded", "none"),
  ellipsis = "...",
  ...
)

\method{paint_format}{logical}(
  x,
  sigfig = 3L,
  max_chars = 12L,
  max_dec_width = 13L,
  subtle_digits = c("insignificant", "rounded", "none"),
  ellipsis = "...",
  ...
)

\method{paint_format}{factor}(
  x,
  sigfig = 3L,
  max_chars = 12L,
  max_dec_width = 13L,
  subtle_digits = c("insignificant", "rounded", "none"),
  ellipsis = "...",
  ...
)

\method{paint_format}{list}(
  x,
  sigfig = 3L,
  max_chars = 12L,
  max_dec_width = 13L,
  subtle_digits = c("insignificant", "rounded", "none"),
  ellipsis = "...",
  ...
)

\method{paint_format}{default}(
  x,
  sigfig = 3L,
  max_chars = 12L,
  max_dec_width = 13L,
  subtle_digits = c("insignificant", "rounded", "none"),
  ellipsis = "...",
  ...
)
}
\arguments{
\item{x}{A vector.}

\item{sigfig}{Significant digits to show in black. Must be in \code{1:15}.}

\item{max_chars}{Strings longer than this are truncated with \code{ellipsis}.}

\item{max_dec_width}{Widest fixed-notation token tolerated before the unit
flips to scientific notation.}

\item{subtle_digits}{Which digits are returned in the grey \code{insig} span.
\code{"insignificant"} greys everything past the \code{sigfig}-th significant digit,
exact or not (so \code{100000} is black \code{100} plus grey \code{000}).
\code{"rounded"} greys only values whose token actually lost precision (so
\code{100000} is entirely black, but \code{123456.789} still greys \code{457.}).
\code{"none"} returns an empty \code{insig} for every value.}

\item{ellipsis}{The truncation marker. Keep it ASCII unless you know your
device can encode the alternative.}

\item{...}{Passed to methods.}
}
\value{
A data frame with one row per element of \code{x} and columns:
\describe{
\item{\code{sig}}{the black span}
\item{\code{insig}}{the grey span, \code{""} when nothing is insignificant}
\item{\code{head}}{the token up to the decimal point}
\item{\code{tail}}{the decimal point and everything after it}
\item{\code{ink}}{\code{"black"} for a finite value, \code{"blue"} for \code{Inf}/\code{NaN},
\code{"red"} for \code{NA}}
\item{\code{align}}{\code{"decimal"}, \code{"left"} or \code{"right"}}
}
with attributes \code{tag} (the type tag) and \code{sci} (did the unit flip to
scientific notation?).
}
\description{
Turns the values of one formatting unit into the spans a painter draws. A
formatting unit is a whole matrix or vector, or one column of a data frame;
\code{paint_format()} is called once per unit, which is exactly why the same value
looks identical in every cell of a matrix.
}
\details{
Numbers are shown at \code{sigfig} significant digits. The digits past the
\code{sigfig}-th are not discarded -- they are returned separately in \code{insig}, so
the renderer can draw them in grey. \code{paste0(sig, insig)} is always exactly the
token that gets drawn.

The whole unit renders in fixed notation, or the whole unit renders in
scientific notation. The decision is made on \strong{width}, not magnitude: if the
widest fixed token would exceed \code{max_dec_width} characters, the unit flips.
In scientific mode \code{insig} is always \code{""}, because every digit of a
scientific token, exponent included, is significant.
}
\examples{
paintr:::paint_format(c(1, 1 / 3))
paintr:::paint_format(123456.789)
paintr:::paint_format(letters[1:3])

}
\keyword{internal}
